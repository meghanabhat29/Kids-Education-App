<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.educationapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".OneManyDashboard">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".PutInPlace"/>
        <activity android:name=".AlphabetPractice"/>
        <activity android:name=".SmallBigLetter"></activity>
        <activity android:name=".Dashboard"></activity>
        <activity android:name=".Article"></activity>
        <activity android:name=".ManyOne"></activity>
        <activity android:name=".AlphabetDashboard"></activity>
        <activity android:name=".Question"></activity>
        <activity android:name=".OneMany"/>
        <activity android:name=".Splashscreen">
        </activity >
        <activity android:name=".LoginActivity"></activity>
        <activity android:name=".MainActivity"></activity>
    </application>

</manifest>